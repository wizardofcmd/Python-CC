class Dog():
    def __init___(self, name, age):
        self.name = name
        self.age = age

    def sit(self):
        print(self.name.titleI() + ' is now sitting.')

    def roll_over(self):
        print(self.name.title() + ' rolled over!')
